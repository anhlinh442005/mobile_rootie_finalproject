<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="0dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="#E6E8E6"
    app:cardBackgroundColor="#FFFFFF">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Icon circle container -->
        <FrameLayout
            android:id="@+id/layoutIconCircle"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:background="@drawable/bg_circle_light_green"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <ImageView
                android:id="@+id/ivGiftIcon"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_gift"
                app:tint="@color/primary" />
        </FrameLayout>

        <!-- Title & Subtitle -->
        <TextView
            android:id="@+id/tvGiftTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="12dp"
            android:fontFamily="sans-serif-medium"
            android:textColor="#1A202C"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/layoutIconCircle"
            app:layout_constraintEnd_toStartOf="@id/tvGiftValue"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Voucher Giảm 50K" />

        <TextView
            android:id="@+id/tvGiftSubtitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="12dp"
            android:fontFamily="sans-serif"
            android:textColor="@color/gray_dark"
            android:textSize="12sp"
            app:layout_constraintStart_toEndOf="@id/layoutIconCircle"
            app:layout_constraintEnd_toStartOf="@id/tvGiftValue"
            app:layout_constraintTop_toBottomOf="@id/tvGiftTitle"
            tools:text="HSD: 15/12/2023" />

        <!-- Price (Right aligned, for exchange tab) -->
        <TextView
            android:id="@+id/tvGiftValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textColor="#1A202C"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="1,200 đ" />

        <!-- Status badge (for my gifts tab) -->
        <TextView
            android:id="@+id/tvGiftStatusBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif"
            android:textSize="11sp"
            android:paddingHorizontal="8dp"
            android:paddingVertical="3dp"
            android:background="@drawable/status_badge_bg"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Còn hạn"
            tools:visibility="visible" />

        <!-- Action Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAction"
            style="@style/Widget.Material3.Button"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:layout_marginTop="12dp"
            android:fontFamily="sans-serif-medium"
            android:text="Đổi quà"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:paddingHorizontal="20dp"
            android:paddingVertical="0dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:backgroundTint="@color/primary"
            app:cornerRadius="6dp"
            app:layout_constraintStart_toStartOf="@id/tvGiftTitle"
            app:layout_constraintTop_toBottomOf="@id/tvGiftSubtitle"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
