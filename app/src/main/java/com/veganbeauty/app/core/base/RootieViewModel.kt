package com.veganbeauty.app.core.base

import androidx.lifecycle.ViewModel

abstract class RootieViewModel : ViewModel() {
    // Base logic for all ViewModels (e.g., error handling, loading states)
}
